@model IEnumerable<A_Market.Models.IdentificationType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdentificationTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdentificationTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdentificationTypeKey }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdentificationTypeKey }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdentificationTypeKey })
        </td>
    </tr>
}

</table>
